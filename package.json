{
  "name": "sopher-ai",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build",
    "vercel-build": "cd frontend && npm ci && npm run build && cd .. && cp -r frontend/.next .next && cp frontend/next.config.js . && if [ -d frontend/public ]; then cp -r frontend/public .; fi"
  },
  "dependencies": {
    "next": "14.2.35",
    "react": "^19.2.4",
    "react-dom": "^18.3.1"
  }
}
