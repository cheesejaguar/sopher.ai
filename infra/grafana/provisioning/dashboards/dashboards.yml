apiVersion: 1

providers:
  - name: 'sopher-dashboards'
    orgId: 1
    folder: 'sopher.ai'
    folderUid: 'sopher'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
