# Core FastAPI and async dependencies
fastapi==0.115.4
uvicorn[standard]==0.32.0
sse-starlette==2.1.3
starlette>=0.41.3  # Fix for denial-of-service vulnerability

# Database and async support
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Caching and session management
redis==5.2.0

# Security and authentication
# NOTE: python-jose has known vulnerabilities but no fix available yet
# Consider migrating to PyJWT in the future
python-jose[cryptography]==3.3.0
cryptography==44.0.1
passlib[bcrypt]==1.7.4
authlib==1.6.1
ecdsa>=0.19.1  # Fix for Minerva attack vulnerability

# Monitoring and metrics
prometheus-client==0.21.0

# AI and LLM integration
openai>=1.51.0,<1.57.0
litellm>=1.49.0  # Update to latest to fix security vulnerabilities
crewai==0.80.0

# HTTP client and utilities
httpx==0.28.0
tenacity==9.0.0
python-multipart==0.0.18
websockets==14.1

# Data validation and settings
pydantic==2.10.2
pydantic-settings==2.6.1

# Build tools (required by crewai)
setuptools>=75.0.0  # Fix for path traversal and RCE vulnerabilities

# PDF processing
pypdf>=5.1.0  # Fix for RAM exhaustion vulnerability