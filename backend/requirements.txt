# Core FastAPI and async dependencies
fastapi==0.115.14
uvicorn[standard]==0.34.3
sse-starlette==2.3.5
starlette>=0.45.3  # Fix for denial-of-service vulnerability

# Database and async support
sqlalchemy[asyncio]==2.0.46
asyncpg==0.30.0
alembic==1.15.2

# Caching and session management
redis==5.3.1

# Security and authentication
PyJWT>=2.10.1  # Replaced python-jose to avoid ecdsa vulnerability
cryptography==45.0.7
passlib[bcrypt]==1.7.4
workos>=5.0.0  # WorkOS SSO integration

# Monitoring and metrics
prometheus-client==0.22.0

# AI and LLM integration
# Using LiteLLM directly instead of CrewAI/LangChain for:
# - 50+ fewer dependencies
# - Faster CI/CD (no complex resolution)
# - Full control over agent behavior
# - Easier debugging (no framework magic)
# - Stability (no surprise breaking changes)
openai>=2.15.0,<3.0.0  # Updated to 2.x for litellm compatibility
litellm>=1.81.0,<2.0.0
instructor==1.8.3

# HTTP client and utilities
httpx>=0.28.1
tenacity>=8.5.0,<10.0.0
python-multipart==0.0.20
websockets==15.0.1

# Data validation and settings
pydantic>=2.12.5
pydantic-settings>=2.8.1

# Build tools
setuptools>=78.1.0  # Fix for path traversal and RCE vulnerabilities

# PDF processing
pypdf>=6.4.0  # Fix for RAM exhaustion vulnerabilities

# Document export formats (EPUB, PDF, DOCX)
ebooklib>=0.18  # EPUB generation
python-docx>=1.1.2  # DOCX generation
reportlab>=4.4.0  # PDF generation
epubcheck>=5.1.0  # EPUB3 validation (requires Java JRE 8+)
